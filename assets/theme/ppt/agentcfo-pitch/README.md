# agentcfo-pitch

- Canvas format: ppt169
- Created: 20260613

## Directories

- `svg_output/`: raw SVG output
- `svg_final/`: finalized SVG output
- `images/`: presentation assets
- `notes/`: speaker notes
- `templates/`: project templates
- `sources/`: source materials and normalized markdown
- `exports/`: local generated PPTX output; do not commit exact duplicates of the canonical deck
- `backup/<timestamp>/`: local generated SVG archive; do not commit when identical to `svg_output/`

The canonical deck is `../agentcfo-pitch.pptx`. Local preview locks, timestamped exports, and identical SVG backups are reproducible and stay out of version control.

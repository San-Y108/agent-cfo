# Architecture decision records

架构决策使用 `000N-kebab-title.md` 命名。

每份 ADR 包含：

- 状态：proposed、accepted、superseded；
- 背景与约束；
- 决策；
- 被拒绝的方案；
- 后果；
- 关联 Issue、PRD 和代码路径。

只记录长期有效且有替代方案的决策。临时任务状态写入 handoff，不写 ADR。

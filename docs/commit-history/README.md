# Commit history batches

这里记录跨多次提交的交付批次，不复制 Git log。

文件命名：

```text
YYYY-MM-DD-<theme>-<batch>.md
```

内容包含：

- 目标；
- commits；
- 影响目录；
- 验证；
- 未完成项；
- 对应 Issue、PRD 与 handoff。

只有用户 Review 通过并明确要求提交后，才写批次记录。

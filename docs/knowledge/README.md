# Knowledge base

这里存放可跨 theme 复用的知识，不存任务状态。

适合内容：

- CAW SDK 使用模式与踩坑；
- DAO Treasury 领域知识；
- mock、testnet、real 的证据规范；
- FastAPI、Next.js、Vercel、Render 的项目级实践；
- 可复用的测试和演示方法。

项目当前技术深文档仍保留在 `docs/backend/`。新知识文件可在此建立摘要并链接原文，避免复制两份事实。

## 索引

| 文件 | 说明 |
|------|------|
| [`project-init.md`](./project-init.md) | 仓库初始化规范（与全局 Cursor skill `project-init` 双向同步；先在此 review，再回写全局） |
| [`readme-polish/`](./readme-polish/) | Phase B 配图 skill 镜像（标杆图 + 反推 Prompt + 脚本；改完后同步回 `%USERPROFILE%\.agents\skills\readme-polish\`） |
